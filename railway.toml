[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile --prod=false && pnpm --filter @repo/api run build"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

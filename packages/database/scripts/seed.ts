import "dotenv/config";

/** Placeholder seed — Thread domain data will be added feature by feature. */
async function main() {
  console.log("Thread seed: nothing to seed yet.");
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
